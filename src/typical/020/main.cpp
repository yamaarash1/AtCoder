#include <iostream>
#include <cstring>
#include <cstdio>
#include <algorithm>
#include <queue>
#include <cmath>
#include <limits>
#include <map>
#include <vector>
#include <set>
#include <iomanip>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  long double a, b, c;
  long double left = pow(c, b);
  long double right = a;
  if (right < left) cout << "Yes" << endl;
  else cout << "No" << endl;
  return 0;
}
