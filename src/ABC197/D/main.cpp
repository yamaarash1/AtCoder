#include <iostream>
#include <cstring>
#include <cstdio>
#include <algorithm>
#include <queue>
#include <cmath>
#include <limits>
#include <map>
#include <vector>
#include <set>
#include <iomanip>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  //long double N;
  //long double x0, y0, xh, yh;
  //cin >> N >> x0 >> y0 >> xh >> yh;
  ////平行移動
  //long double x0d = 0, y0d = 0, xhd = xh - x0, yhd = yh - y0;
  ////直行座標を(xhd, yhd)を極座標(r, theta)に変換
  //long double r = sqrt((x0 - xh) * (x0 - xh) + (y0 - yh) * (y0 - yh));
  //long double theta = acos(xhd / r);
  //long double r1 = r * sin(theta);
  //long double x1 = (r * sin(theta) * cos(N * theta / 2)) + x0;
  //long double y1 = (r * sin(theta) * sin(N * theta / 2)) + y0;

  //cout << "r: " << r << endl;
  //cout << "theta: " << theta << endl;
  //cout << "r1: " << r1 << endl;
  //cout << "x1: " << x1 << endl;
  //cout << "y1: " << y1 << endl;

  //cout << fixed << setprecision(10) << x1 << " " << y1 << endl;

  return 0;
}
