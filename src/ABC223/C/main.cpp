#include <iostream>
#include <cstring>
#include <cstdio>
#include<algorithm>
#include <queue>
#include <cmath>
#include <limits>
#include <map>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
double N = pow(10, -6);

int main() {
  int N; 
  cin >> N;
  double a[N], b[N];
  rep(i, N) cin >> a[i] >> b[i];
  double sec = N;
  return 0;
}
